package parser

import "testing"

func TestParse2(t *testing.T) {
	// pairs := Tokenizer("A || B")
	// parser := Parse(pairs)

	// // assert.Equal(t, parser.Len(), 1)
	// log.Println(parser.String())
	// log.Printf("%#v", parser.Parse())
}
